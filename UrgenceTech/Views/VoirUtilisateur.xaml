<Page x:Class="UrgenceTech.Views.VoirUtilisateur"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UrgenceTech.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VoirUtilisateur"
      Background="Transparent">


    <Grid MouseMove="OnUserActivity" KeyDown="OnUserActivity">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <TextBox x:Name="StatusText"
    TextAlignment="Center" Margin="634,-399,-46,786" RenderTransformOrigin="0.52,0.484"/>

        <Border Grid.ColumnSpan="3" Grid.Column="1" Width="200" Height="60" Background="White">
            <TextBlock   Text="Consulter les utilisateurs" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <Button Content="Déconnexion" Width="100" Height="20" Click="OnLogout"
     Grid.Column="5" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top"/>

        <TextBox Grid.ColumnSpan="3" Grid.Column="1" Grid.Row="1" Width="300" Height="40" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                 TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <DataGrid ItemsSource="{Binding Utilisateurs}" Grid.ColumnSpan="3" Grid.RowSpan="3" ColumnWidth="*"
                  Grid.Column="1" Grid.Row="2" AutoGenerateColumns="False">

            <!--Si Status = False, le row devien rouge-->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="False">
                            <Setter Property="Background" Value="#FFFFC0C0"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>

                <DataGridTextColumn Header="Nom complet" Binding="{Binding NomComplet}"/>
                <DataGridTextColumn Header="Courriel" Binding="{Binding Courriel}"/>
                <DataGridTextColumn Header="Rôle" Binding="{Binding Role}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>
