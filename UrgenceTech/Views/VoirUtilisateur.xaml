<Page x:Class="UrgenceTech.Views.VoirUtilisateur"
      x:ClassModifier="internal"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UrgenceTech.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VoirUtilisateur"
      Background="#F5F5F5">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Consultation d'utilisateur" FontSize="26" FontWeight="Bold" Foreground="#1976D2"  Grid.ColumnSpan="3"/>

        <Border Grid.Column="2" Background="White" Margin="0,0,0,70" Grid.Row="1" CornerRadius="8">
            <TextBlock   Text="Barre de recherche" VerticalAlignment="Center" HorizontalAlignment="Center" Background="#E0E0E0"/>
        </Border>

        <TextBox Grid.ColumnSpan="3" Grid.Column="1" Grid.Row="1" Width="300" Height="40" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                 TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center"/>



        <Border Grid.RowSpan="4" Grid.Column="1" BorderThickness="10" Margin="6,80,154,10" Background="White" BorderBrush="#E0E0E0" CornerRadius="8" Grid.ColumnSpan="4" Grid.Row="1">
            <DataGrid ItemsSource="{Binding Utilisateurs}" AutoGenerateColumns="False" ColumnWidth="*">

                <!--Si Status = False, le row devien rouge-->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="False">
                                <Setter Property="Background" Value="#FFFFC0C0"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Nom complet" Binding="{Binding NomComplet}"/>
                    <DataGridTextColumn Header="Courriel" Binding="{Binding Courriel}"/>
                    <DataGridTextColumn Header="Rôle" Binding="{Binding Role}"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>

                </DataGrid.Columns>

            </DataGrid>
        </Border>
    </Grid>
</Page>



