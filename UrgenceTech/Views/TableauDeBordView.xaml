<UserControl x:Class="UrgenceTech.Views.TableauDeBordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F5F5F5" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Tableau de bord"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#1976D2"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8"
                Padding="25"
                Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <Border Background="#E3F2FD" CornerRadius="8" Padding="20" MinWidth="180">
                    <StackPanel>
                        <TextBlock Text="Urgences actives"
                                   FontSize="13"
                                   Foreground="#546E7A"/>
                        <TextBlock Text="{Binding NombreUrgencesActives}"
                                   FontSize="40"
                                   FontWeight="Bold"
                                   Foreground="#1976D2"/>
                    </StackPanel>
                </Border>
                
                <Border Background="#E8F5E9" CornerRadius="8" Padding="20" MinWidth="180" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Résolues aujourd'hui"
                       FontSize="13"
                       Foreground="#546E7A"/>
                        <TextBlock Text="{Binding NombreUrgencesResoluesAujourdhui}"
                       FontSize="40"
                       FontWeight="Bold"
                       Foreground="#388E3C"/>
                    </StackPanel>
                </Border>

                <Border Background="#FFF3E0" CornerRadius="8" Padding="20" MinWidth="180">
                    <StackPanel>
                        <TextBlock Text="Utilisateurs actifs"
                       FontSize="13"
                       Foreground="#546E7A"/>
                        <TextBlock Text="{Binding NombreUtilisateursConnectes}"
                       FontSize="40"
                       FontWeight="Bold"
                       Foreground="#F57C00"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8"
                Padding="25">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Urgences récentes"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Margin="0,0,0,15"/>

                <ListView Grid.Row="1"
                          ItemsSource="{Binding UrgencesRecentes}"
                          BorderThickness="0"
                          Background="Transparent">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Titre" Width="250"
                                            DisplayMemberBinding="{Binding Titre}"/>
                            <GridViewColumn Header="Priorité" Width="100"
                                            DisplayMemberBinding="{Binding Priorite}"/>
                            <GridViewColumn Header="Statut" Width="100"
                                            DisplayMemberBinding="{Binding Statut}"/>
                            <GridViewColumn Header="Date" Width="150"
                                            DisplayMemberBinding="{Binding DateCreation, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"/>
                            <GridViewColumn Header="Créé par" Width="150"
                                            DisplayMemberBinding="{Binding Utilisateur.NomComplet}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <TextBlock Grid.Row="1"
                           Text="Aucune urgence active."
                           FontSize="14"
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UrgencesRecentes.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

            </Grid>
        </Border>

    </Grid>
</UserControl>